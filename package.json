{
  "name": "dokad-random-city-explorer",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node test/geo-test.js",
    "deploy": "vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "playwright": "^1.62.1",
    "vite": "^6.2.0"
  },
  "dependencies": {
    "maplibre-gl": "^6.6.0"
  }
}
